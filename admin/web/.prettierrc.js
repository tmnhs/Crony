module.exports = {
	// 超过多少字符后换行
	printWidth: 120,
	// 行末分号
	semi: false,
	// 单引号
	singleQuote: true,
	// 缩进
	tabWidth: 2,
	// 使用tab缩进还是空格
	useTabs: true,
	// 是否使用尾逗号
	trailingComma: 'es5',
	// > 标签放在最后一行的末尾，而不是单独放在下一行
	jsxBracketSameLine: false,
	// (x) => {} 箭头函数参数只有一个时是否要有小括号。 alwaysz:总是带括号，avoid：省略括号
	arrowParens: 'avoid',
}
